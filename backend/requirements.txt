# Intelligent Investor Pro - Backend Dependencies
# Python 3.11+ required

# === Core Framework ===
fastapi==0.115.6                    # High-performance async API framework
uvicorn[standard]==0.34.0           # ASGI server with hot reload
pydantic==2.10.4                    # Data validation & serialization
pydantic-settings==2.7.1            # Environment configuration

# === Data Processing ===
pandas==2.2.3                       # DataFrame operations for CSV/JSON
orjson==3.10.13                     # Fast JSON serialization

# === Environment ===
python-dotenv==1.0.1                # Load .env files

# === Real-Time Data ===
yfinance==0.2.50                    # Yahoo Finance API for real-time prices

# === Caching ===
diskcache==5.6.3                    # File-based caching with TTL support

# === AI Integration ===
google-genai==1.56.0                # Google Gemini API SDK (current; replaces google-generativeai)

# === Rate Limiting ===
slowapi==0.1.9                      # Rate limiting for FastAPI

# === HTTP Client ===
httpx==0.28.1                       # Async HTTP client for health checks

# === Observability ===
python-json-logger>=3.1,<4          # Structured JSON logging
prometheus-client>=0.21,<1          # Metrics exposition
sentry-sdk[fastapi]>=2.0,<3         # Error tracking (active only when SENTRY_DSN set)

# === Persistence ===
sqlalchemy[asyncio]>=2.0,<3         # Async ORM
asyncpg>=0.30,<1                    # Postgres async driver
alembic>=1.14,<2                    # Schema migrations
redis>=5.2,<6                       # Cache / queue backend
arq>=0.26,<0.27                     # Async task queue (off-request AI pipeline)
